<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Telephones List</title>
        <link href="./static/styles.css"  rel="stylesheet" type="text/css" >
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="./static/script.js"></script>
    </head>
    <body>
        <div class="main-container">
            <h1 class="main-title">PHONE BOOK</h1>
            {{#if disabled}}
                <script>
                    $(document).ready(function () {
                        $(".submit-button-del").attr("disabled", true);
                    })
                </script>

                <div class="notes-container">
                    {{#each notes}}
                        {{> list_btn}}
                    {{/each}}
                </div>

            {{else}}
                <div class="notes-container">
                    {{#each notes}}
                        {{> list_btn}}
                    {{/each}}
                </div>
            {{/if}}


            <div class="partial-content">
                {{#if add_btn}}
                    {{> add_btn}}
                {{/if}}
                {{#if upd}}
                    {{#if disabled}}
                            <script>
                                $(document).ready(function () {
                                    $(".submit-button-del").attr("disabled", false);
                                })
                            </script>
                        {{/if}}
                {{> update_block}}
                {{/if}}
                {{#if add}}
                {{> add_block}}
                {{/if}}
            </div>
        </div>

    </body>
</html>
